org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# The supplied Windows workspace contains Thai characters. Allow this path;
# if a native tool still fails, build from an ASCII-only checkout (see README).
android.overridePathCheck=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin incremental caches cannot relativize C: Pub sources against this D: project.
# Disable only Kotlin incremental compilation; Gradle caching remains enabled.
kotlin.incremental=false
